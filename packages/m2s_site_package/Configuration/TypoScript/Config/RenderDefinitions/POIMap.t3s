tt_content {
    m2s_poi_map =< lib.M2S_ELEMENT
    m2s_poi_map {
        templateName = Plugin

        variables {
            plugin = USER
            plugin {
                userFunc = TYPO3\CMS\Extbase\Core\Bootstrap->run
                extensionName = PoiMap
                pluginName = Places
                vendorName = M2S
            }
        }
    }
}